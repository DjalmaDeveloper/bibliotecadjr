# name: CI - Build backend and Deploy frontend
# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]

# jobs:
#   build-backend:
#     name: Build backend (Maven)
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Java 17
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '17'
#           cache: 'maven'

#       - name: Build backend with Maven
#         working-directory: backend/sistema-biblioteca
#         run: mvn -B clean package -DskipTests

#       - name: Upload backend JAR artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: backend-jar
#           path: backend/sistema-biblioteca/target/*.jar

#   deploy-frontend:
#     name: Deploy frontend (GitHub Pages)
#     needs: build-backend
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       # If your frontend needs a build step (npm/webpack), add setup-node and build commands here.
#       # This repository contains static HTML/JS/CSS in the `frontend` folder.

#       # Prefer Cloudflare Pages deployment when Cloudflare secrets are present.
#       - name: Deploy frontend to Cloudflare Pages
#         if: ${{ secrets.CLOUDFLARE_API_TOKEN != '' && secrets.CLOUDFLARE_ACCOUNT_ID != '' }}
#         uses: cloudflare/wrangler-action@v3
#         with:
#           apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#           accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
#           # Deploy the `frontend` directory to your Cloudflare Pages project named 'bibliotecadjr'
#           command: pages deploy ./frontend --project-name=bibliotecadjr

#       # Fallback to GitHub Pages if Cloudflare secrets are not configured
#       - name: Deploy frontend to GitHub Pages
#         if: ${{ secrets.CLOUDFLARE_API_TOKEN == '' || secrets.CLOUDFLARE_ACCOUNT_ID == '' }}
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./frontend
#           publish_branch: gh-pages

#   package-and-release:
#     name: Optional - Create Release with backend artifact
#     needs: build-backend
#     runs-on: ubuntu-latest
#     if: startsWith(github.ref, 'refs/tags/')
#     steps:
#       - name: Download built artifact
#         uses: actions/download-artifact@v4
#         with:
#           name: backend-jar

#       - name: Create Release
#         uses: softprops/action-gh-release@v1
#         with:
#           files: '*.jar'
      
#       - name: Deploy to CloudFlare Pages
#         uses: cloudflare/wrangler-action@v3
#         with:
#           accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
#           apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#           command: pages deploy publish/wwwroot --project-name=bibliotecadjr